import { SCHOOL_LIST } from '@/lib/schools';

export default function Page() {
  const schools = SCHOOL_LIST;
  return (
    <main className="mx-auto max-w-3xl px-4 py-8">
      <h1 className="text-2xl font-semibold mb-4">Schools</h1>
      <ul className="space-y-2">
        {schools.map(s=>(
          <li key={s.unitid} className="border rounded px-3 py-2 flex items-center justify-between">
            <span>{s.name}{s.isCommunityCollege ? ' *' : ''}</span>
            <a className="text-blue-600 underline" href={`/u/${s.unitid}`}>Open</a>
          </li>
        ))}
      </ul>
    </main>
  )
}
